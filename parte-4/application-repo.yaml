apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-bitnami
  namespace: argocd
spec:
  project: default
  destination:
    name: in-cluster
    namespace: nginx-bitnami
  sources:
    - repoURL: https://charts.bitnami.com/bitnami
      chart: nginx
      targetRevision: 15.6.0
      helm:
        valueFiles:
          - $values/parte-4/values/values.yaml
    - repoURL: https://github.com/LaloMiranda/argocd-workshop
      targetRevision: feat_parte-4
      ref: values
  # syncPolicy:
  #   automated:
  #     prune: true
  #     selfHeal: true
